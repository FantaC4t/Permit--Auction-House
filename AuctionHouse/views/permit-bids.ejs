<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Permit Bids</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f3f4f6;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .bid {
            padding: 10px;
            border-bottom: 1px solid #ccc;
        }
        .bid:last-child {
            border-bottom: none;
        }
    </style>
</head>
<body>
    <nav>
        <div><a href="/">Shop</a></div>
        <div><a href="/logout">Logout</a></div>
    </nav>

    <div class="container">
        <h1>Bids for <%= permit.name %></h1>
        <p><%= permit.description %></p>
        <h2>Bids</h2>
        <% if (bids.length > 0) { %>
            <% bids.forEach(bid => { %>
                <div class="bid">
                    <p>Bidder: <%= bid.bidder.username %></p>
                    <p>Amount: <%= bid.amount %></p>
                    <p>Time: <%= bid.bidTime.toLocaleString() %></p>
                </div>
            <% }) %>
        <% } else { %>
            <p>No bids yet.</p>
        <% } %>
        <a href="/">Back to Shop</a>
    </div>
</body>
</html>